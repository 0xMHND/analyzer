Run add_prints.py script to add print statements after each datablock allocation, destruction, or release

	python3 add_prints.py <filename.c>

THIS WILL MODIFY THE .c FILE! BACKUP BACKUP BACKUP

Compile and run the program as usual using OCR
Save stdout to a text file using redirection for later analysis
